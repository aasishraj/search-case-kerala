[project]
name = "kerala-courts-case-search-api"
version = "0.1.0"
description = "FastAPI wrapper for Kerala High Court case search and document retrieval."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "pydantic>=2.12.5",
    "requests>=2.32.5",
]
